/**
 * Type utility functions to work with complex entity types
 * @module shared/common/type-utils
 */

import { 
  ClientEntity, 
  EntityBase, 
  EntityMetadata
} from "@shared/entities";

// No additional utility types needed anymore since we've updated the base types
// This file is kept for future utility types that might be needed 