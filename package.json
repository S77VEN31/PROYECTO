{
  "name": "colori-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "build:shared": "cd shared && cross-env NODE_OPTIONS=\"--max-old-space-size=4096\" npm run build",
    "build:backend": "cd backend && cross-env NODE_OPTIONS=\"--max-old-space-size=4096\" npm run build",
    "build:frontend": "cd frontend && cross-env NODE_OPTIONS=\"--max-old-space-size=4096\" npm run build",
    "build": "npm run build:shared && npm run build:backend && npm run build:frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "dependencies": {
    "zod": "^3.25.17"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "cross-env": "^7.0.3"
  }
}
